package cp16_リストと操作
/*
 * リストの操作
 * リストリテラル
 * ※リストは配列と似ているが、イミュータブル(各要素に代入不可)であり、構造が再帰的である。
 */

object No16_1 extends App {

    val fruit = List("apples", "oranges", "pears")

    val nums = List(1, 2, 3, 4)

    val diag3 = List(
        List(1, 0, 0),
        List(0, 1, 0),
        List(0, 0, 1))

    val empty = List()
}
